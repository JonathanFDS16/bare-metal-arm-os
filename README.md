# Bare-Metal Simple OS Kernel for Cortex-M3

A simple, bare-bones, (probably buggy) kernel written for Cortex-M3

I wanted to learn OS actually works, and I came up with the idea to code a prototype of one for a simple microcontroller.

## What does it do?

I was able to implement some stuff:
    1. USART I/O so we can send and receive data to the terminal (i.e write to the terminal)
    2. We can malloc, but we can't free (yet). So careful with your allocations :D
    3. Context Switching! Threads can be created.
    4. RR Scheduler by using the chip's SysTick
    5. Primitive shell with two commands `reboot` and `help`
    6. Primitive syscall abilities

## Future Plans

- Organize the code structure more.
- Populate a user-space libraries of syscalls for user programs to use 
- Ability to free memory
- Add more shell commands (maybe little file system)
- TCP/IP Stack (This is BIG, I might change to a better chip to have ethernet)

## OBS

I am not a proficient C developer (yet), so expect errors. 
I tried creating the simplest prototype so I could learn about the main concepts, thus I am aware of many security vulnerabilities (i.e buffer overflow).

## AI Usage

I used AI mostly as a guide/professor and debugger when I was really stuck. 
Code was generated by my hands because I want to learn the concepts.
