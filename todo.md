Cool todos:
1. malloc and free
    - So malloc and free are library function that wrap around syscalls from OS
    - Therefore I do need to create a malloc syscall with trap handlers and return a pointer (simpler than the computer ones)
2. running two different processes, savings stack and etc
3. how would I create a syscall into this kernel so user programs can call and receive something back? Trap instruction (Learned)
4. write syscall (I kinda know now)
